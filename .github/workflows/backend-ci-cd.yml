name: Backend CI/CD

on:
  push:
    branches:
      - main
    paths:
      - "backend/**"
  pull_request:
    branches:
      - main
    paths:
      - "backend/**"

jobs:
  test-build:
    name: Test and Build
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: 18

      - name: Install dependencies
        working-directory: backend
        run: npm install

      - name: Run tests
        working-directory: backend
        run: npm test


  push-docker:
    name: Push Docker images
    needs: test-build
    if: github.event_name == 'push'
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Login Docker Hub
        uses: docker/login-action@v4
        with:
          username: ${{ vars.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build images
        run: |
          docker build \
            -t ${{ vars.DOCKERHUB_USERNAME }}/backend:latest \
            -t ${{ vars.DOCKERHUB_USERNAME }}/backend:${{ github.sha }} \
            ./backend

      - name: Push latest image
        run: docker push ${{ vars.DOCKERHUB_USERNAME }}/backend:latest

      - name: Push sha image
        run: docker push ${{ vars.DOCKERHUB_USERNAME }}/backend:${{ github.sha }}
